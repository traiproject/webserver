version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/app/db/queries"
    schema: "internal/app/db/migrations"
    gen:
      go:
        emit_json_tags: true
        package: "store"
        out: "internal/app/db/store"
        sql_package: "pgx/v5"
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"

          - db_type: "timestamptz"
            go_type:
              import: "time"
              type: "Time"
